---
import DocsLayout from "../../layouts/DocsLayout.astro";
---

<DocsLayout title="常见问题" description="DocMind 常见问题解答">
  <h1>常见问题</h1>

  <h2>购买与激活</h2>

  <h3>怎么买 Pro?</h3>
  <p>打开 DocMind 客户端 → 顶栏点 Free / Trial 状态条 → 在弹出的升级对话框点「立即购买」→ 客户端内直接显示微信支付二维码 → 手机微信扫码付 <strong>¥20</strong> → 支付成功后客户端 <strong>自动激活</strong>,顶栏直接变成 👑 Pro。整个流程不离开 DocMind 窗口,不需要复制粘贴任何激活码。</p>

  <h3>支付完没自动激活,怎么办?</h3>
  <p>微信支付偶尔有回调延迟,先等 1-2 分钟。如果还没激活:</p>
  <ul>
    <li>退出客户端再启动,启动时会主动查一次订单状态并补发激活</li>
    <li>仍然没好,联系 <a href="mailto:qdzy_cai@163.com">qdzy_cai@163.com</a> 并提供订单号(微信支付凭证里能看到)</li>
  </ul>

  <h3>License 可以用几台设备?</h3>
  <p>每个 license 绑定 <strong>一台</strong>设备 — 通过硬件指纹(主板序列号 + 网卡 MAC 等的 SHA-256 哈希)识别。换电脑或重装系统后指纹会变,需要联系客服补发新设备的激活 token。</p>

  <h3>没网络的电脑能激活吗?</h3>
  <p>能。在客户端的「升级 → 离线激活」页复制设备指纹,把指纹通过微信 / 邮件发给客服,客服会按指纹手工签发一段 token JSON 给你,粘贴到客户端即可激活,全程不需要客户端联网。</p>

  <h3>试用版会自动变成 Pro 吗?</h3>
  <p>不会。试用是 5 天临时解锁,到期回到 Free。<strong>购买 Pro 需要单独操作</strong>(顶栏点 Trial 状态条 → 立即购买)。</p>

  <h3>如何申请退款?</h3>
  <p>购买后 7 天内可申请全额退款,联系 <a href="mailto:qdzy_cai@163.com">qdzy_cai@163.com</a> 并提供订单号。退款成功后该 license 自动失效,客户端会自动回退到 Free / Trial。</p>

  <h2>隐私与数据</h2>

  <h3>DocMind 会上传我的文档吗?</h3>
  <p>不会。所有文档内容、索引、搜索查询、AI 问答都保存在你的本地磁盘,DocMind 没有任何云端服务接收这些数据。详见 <a href="/privacy">隐私政策</a>。</p>

  <h3>AI 模型在本地还是云端?</h3>
  <p>本地。DocMind 使用 llama.cpp 加载 GGUF 模型在 CPU/GPU 上推理,Embedding 用 ONNX Runtime 跑 BGE 模型。除非你主动启用了"API 模式",否则不会调任何云端 AI API。</p>

  <h3>可以完全离线/在内网使用吗?</h3>
  <p>可以。Pro license 一旦激活,客户端不再联网调用任何服务。模型可以提前下载好放进对应目录。</p>

  <h2>性能与卡顿</h2>

  <h3>索引很慢怎么办?</h3>
  <p>初始扫描的速度主要受文件数量和 PDF/Office 解析速度影响。一般指标:</p>
  <ul>
    <li>纯文本文件:1000+ 个/秒</li>
    <li>PDF:5-20 页/秒(取决于 PDF 复杂度)</li>
    <li>Office:20-50 个/秒</li>
  </ul>
  <p>如果电脑发热严重,DocMind 会自动降速。索引在后台进行,不影响搜索使用。</p>

  <h3>AI 问答很慢?</h3>
  <p>看一下加载的模型档位:</p>
  <ul>
    <li>4B 模型在 8GB 内存机器上会非常慢,建议换 1.7B</li>
    <li>Windows 没有 GPU 加速,默认 CPU 推理,速度约为 macOS Metal 的 1/3</li>
    <li>如果机器内存吃紧,问答会更慢甚至失败 — 关闭其他程序释放内存</li>
  </ul>

  <h3>占内存多大?</h3>
  <p>空闲时 DocMind 大约占 200-400 MB(取决于索引大小)。加载 LLM 后:</p>
  <ul>
    <li>0.6B 模型:+700 MB</li>
    <li>1.7B 模型:+1.5 GB</li>
    <li>4B 模型:+3 GB</li>
  </ul>

  <h2>功能与限制</h2>

  <h3>支持哪些文件格式?</h3>
  <p>主要类别:</p>
  <ul>
    <li><strong>办公文档</strong>:PDF、Word (.docx / .doc)、Excel (.xlsx / .xls / .csv)、PowerPoint (.pptx / .ppt)、RTF</li>
    <li><strong>电子书</strong>:EPUB</li>
    <li><strong>文本 / 标记</strong>:TXT、Markdown、RST</li>
    <li><strong>代码 / 配置</strong>:py / js / ts / java / go / rs / c / cpp / swift / kt / rb / php / yaml / json / xml / sh / sql 等 40+ 种</li>
    <li><strong>归档</strong>:ZIP(自动解析内部文件)</li>
    <li><strong>图片</strong>(Pro):JPG / PNG / BMP / TIFF / WebP / HEIC,用系统原生 OCR 识别中英文</li>
  </ul>

  <h3>能搜中文 PDF 吗?</h3>
  <p>能。DocMind 优先用 pdfium 提取文本层(支持中文),fallback 到 pdf-extract。完全无文字层的扫描型 PDF 需要 Pro 版的 OCR 功能。</p>

  <h3>支持哪些操作系统?</h3>
  <p>macOS 12+(Apple Silicon 与 Intel)、Windows 10/11 64 位。Linux 暂未发布安装包,但代码本身可在 Linux 编译运行。</p>

  <h2>故障排查</h2>

  <h3>初次安装在 macOS 上提示"无法验证开发者"</h3>
  <p>DocMind 暂未做 Apple 开发者证书签名。在 系统设置 → 隐私与安全性 → 仍要打开 即可。</p>

  <h3>下载模型失败</h3>
  <p>检查网络。语义模型从 ModelScope 下载,LLM 模型可从 ModelScope 或 HuggingFace。代理用户请确认代理白名单包含这两个域名。</p>

  <h3>怎么彻底重置 DocMind?</h3>
  <p>退出应用,删除以下目录:</p>
  <ul>
    <li>macOS: <code>~/Library/Application Support/com.caizhongrui.docmind/</code></li>
    <li>Windows: <code>%APPDATA%\com.caizhongrui.docmind\</code></li>
  </ul>
  <p>下次启动会从全新状态开始。</p>

  <h2>还有其他问题?</h2>
  <p><a href="/contact">联系我们</a>,我们会认真回复每一封邮件。</p>
</DocsLayout>
