---
import DocsLayout from "../../layouts/DocsLayout.astro";
---

<DocsLayout title="常见问题" description="DocMind 常见问题解答">
  <h1>常见问题</h1>

  <h2>购买与激活</h2>

  <h3>买完后怎么收到 license key?</h3>
  <p>支付成功后,PayJS 回调 DocMind 服务器,服务器立即生成一个 license key 并通过支付完成页展示给你。请保存好该 key,后续需要在 DocMind 应用内输入它来激活。</p>

  <h3>License 可以用几台设备?</h3>
  <p>每个 license 只能激活 <strong>一台</strong> 设备,且激活后无法转移。请在你的常用设备上完成激活。</p>

  <h3>激活时输入 key 报错"DEVICE_BOUND"?</h3>
  <p>说明该 key 已经在另一台设备上激活过。我们不提供解绑服务,需要在新设备上重新购买。</p>

  <h3>如何申请退款?</h3>
  <p>购买后 7 天内、license 未激活时可申请全额退款。激活后即视为已交付,不再退款。退款请联系 <a href="mailto:qdzy_cai@163.com">qdzy_cai@163.com</a> 并提供订单号。</p>

  <h2>隐私与数据</h2>

  <h3>DocMind 会上传我的文档吗?</h3>
  <p>不会。所有文档内容、索引、搜索查询、AI 问答都保存在你的本地磁盘,DocMind 没有任何云端服务接收这些数据。详见 <a href="/privacy">隐私政策</a>。</p>

  <h3>AI 模型在本地还是云端?</h3>
  <p>本地。DocMind 使用 llama.cpp 加载 GGUF 模型在 CPU/GPU 上推理,Embedding 用 ONNX Runtime 跑 BGE 模型。除非你主动启用了"API 模式",否则不会调任何云端 AI API。</p>

  <h3>可以完全离线/在内网使用吗?</h3>
  <p>可以。Pro license 一旦激活,客户端不再联网调用任何服务。模型可以提前下载好放进对应目录。</p>

  <h2>性能与卡顿</h2>

  <h3>索引很慢怎么办?</h3>
  <p>初始扫描的速度主要受文件数量和 PDF/Office 解析速度影响。一般指标:</p>
  <ul>
    <li>纯文本文件:1000+ 个/秒</li>
    <li>PDF:5-20 页/秒(取决于 PDF 复杂度)</li>
    <li>Office:20-50 个/秒</li>
  </ul>
  <p>如果电脑发热严重,DocMind 会自动降速。索引在后台进行,不影响搜索使用。</p>

  <h3>AI 问答很慢?</h3>
  <p>看一下加载的模型档位:</p>
  <ul>
    <li>4B 模型在 8GB 内存机器上会非常慢,建议换 1.7B</li>
    <li>Windows 没有 GPU 加速,默认 CPU 推理,速度约为 macOS Metal 的 1/3</li>
    <li>如果机器内存吃紧,问答会更慢甚至失败 — 关闭其他程序释放内存</li>
  </ul>

  <h3>占内存多大?</h3>
  <p>空闲时 DocMind 大约占 200-400 MB(取决于索引大小)。加载 LLM 后:</p>
  <ul>
    <li>0.6B 模型:+700 MB</li>
    <li>1.7B 模型:+1.5 GB</li>
    <li>4B 模型:+3 GB</li>
  </ul>

  <h2>功能与限制</h2>

  <h3>支持哪些文件格式?</h3>
  <p>PDF、Word(.docx/.doc)、Excel(.xlsx/.xls/.csv)、PowerPoint(.pptx/.ppt)、Markdown、TXT、RTF、ZIP(自动解析内部文件)。Pro 用户额外支持 OCR 索引图片和扫描 PDF(JPG/PNG/BMP/TIFF/WebP)。</p>

  <h3>能搜中文 PDF 吗?</h3>
  <p>能。DocMind 优先用 pdfium 提取文本层(支持中文),fallback 到 pdf-extract。完全无文字层的扫描型 PDF 需要 Pro 版的 OCR 功能。</p>

  <h3>支持哪些操作系统?</h3>
  <p>macOS 12+(Apple Silicon 与 Intel)、Windows 10/11 64 位。Linux 暂未发布安装包,但代码本身可在 Linux 编译运行。</p>

  <h2>故障排查</h2>

  <h3>初次安装在 macOS 上提示"无法验证开发者"</h3>
  <p>DocMind 暂未做 Apple 开发者证书签名。在 系统设置 → 隐私与安全性 → 仍要打开 即可。</p>

  <h3>下载模型失败</h3>
  <p>检查网络。语义模型从 ModelScope 下载,LLM 模型可从 ModelScope 或 HuggingFace。代理用户请确认代理白名单包含这两个域名。</p>

  <h3>怎么彻底重置 DocMind?</h3>
  <p>退出应用,删除以下目录:</p>
  <ul>
    <li>macOS: <code>~/Library/Application Support/com.caizhongrui.docmind/</code></li>
    <li>Windows: <code>%APPDATA%\com.caizhongrui.docmind\</code></li>
  </ul>
  <p>下次启动会从全新状态开始。</p>

  <h2>还有其他问题?</h2>
  <p><a href="/contact">联系我们</a>,我们会认真回复每一封邮件。</p>
</DocsLayout>
